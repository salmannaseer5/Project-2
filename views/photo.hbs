<div class="card">
  <div class="card-content">
    <a class="card-title" href="/photo/{{id}}">
      {{ title }}
    </a>
  </div>
  <div class="card-action">
    <span class="tweet__date">{{ createdAt }}</span>
    <span class="tweet_comments">{{ comments.length }} Comments</span>
  </div>
</div>